# 依赖
frontend/node_modules/
backend/web/dist/
*.db
data/

# 本地二进制（单文件交付物，可经 build.sh 重建）
/dist/
*.exe
/swb

# 环境
.env

# 本地运行产物（测试密钥 / 日志，勿提交）
secrets.env
runtime.log

# Python（tools/pack_extension.py 运行/编译产生的字节码缓存）
# 不忽略会导致 release.sh 前置的「工作区干净」检查失败，从而挡住发版。
__pycache__/
*.pyc
*.pyo
