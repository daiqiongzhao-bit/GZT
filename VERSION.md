# GZT 版本规则

采用语义化版本 `v主版本.次版本.修订号`，格式固定为 **小写 v + 三位数字**（如 `v0.6.1`）。

## 版本号含义

| 变更类型 | 版本位 | 示例 | 说明 |
|---|---|---|---|
| 初始开发版 | — | `v0.0.1` | 项目初始版本 |
| **Bug 修复** | 修订号 +1 | `v0.6.0` → `v0.6.1` | 修缺陷、改文案、调样式、性能优化，不含新功能 |
| **新增功能** | 次版本 +1，修订号归零 | `v0.6.0` → `v0.7.0` | 新增模块、新增接口、新增页面 |
| **正式稳定版** | 主版本 +1，其余归零 | `v0.6.0` → `v1.0.0` | 功能冻结并稳定运行一段时间后发布 |

## 判定原则

- **一次发布只做一件事**：修复和功能不要混在同一个版本里。若同一批改动两者都有，拆成两次发布（先发修复版，再发功能版）。
- **破坏性变更**（数据库结构变更、接口不兼容、配置项改名）即使是修复，也应走次版本 +1，并在 release notes 中显著标注。
- **`latest` 标签始终指向最新稳定构建**，容器默认使用 `latest`。

## 发布方式

使用项目根目录的 `release.sh`，不要手动打 tag：

```bash
./release.sh patch            # 修复：v0.6.0 -> v0.6.1
./release.sh minor            # 功能：v0.6.0 -> v0.7.0
./release.sh major            # 正式：v0.6.0 -> v1.0.0
./release.sh minor --deploy   # 顺带重启线上容器（线上会短暂中断）
```

脚本会自动完成：校验工作区干净 → 校验分支与远端同步 → 计算版本号 → 打 tag
→ 推送 GitHub → 构建镜像 → 推送 Docker Hub（版本标签 + `latest`）。

**不加 `--deploy` 时只推送镜像，不重启容器**，线上仍运行旧版本，可自行择时上线。

## v1.0.0 正式版验收标准

达到以下全部条件后，发布 `v1.0.0`：

- [ ] 时区处理稳定（无跨时区任务/推送时间错误）
- [ ] 推送功能稳定（当班提醒、艾特、去重均正常）
- [ ] 权限与在线用户监控稳定
- [ ] 数据备份与恢复验证通过
- [ ] 连续 2 周无 P0 级缺陷，且期间只发过修复版

## AES 密钥的历史遗留问题（2026-08-29 已修复）

容器启动若没注入 `AES_KEY`，代码会静默回退到默认值 `shift-workbench-aes-key-2024`。
**这套系统早期就是这样跑的**，所以库里的加密数据（Webhook 地址、SMTP 密码、备份远端目录）
很长一段时间都是用默认密钥加密的。

2026-08-29 修正密钥注入后，应用改用 `/opt/swb/swb.env` 里的真密钥解密，
解不开历史密文 → 企业微信推送报「解密失败」，提醒直接发不出去。

**已处理**：把 Webhook 地址用默认密钥解出、改用真密钥重新加密后写回（改动前已备份数据库）。
其余加密字段当时均为空，无需迁移。

**以后若再遇到「解密失败」**，说明密文与当前密钥不匹配，迁移方法：

```python
# 用旧密钥解出明文，再用 /opt/swb/swb.env 里的 AES_KEY 重新加密写回。
# 格式与 db/crypto.go 一致：base64(nonce12 + ciphertext + tag16)，AES-256-GCM。
```

涉及字段：`webhooks.url`、`webhooks.secret`、`settings.smtp_pass`、备份配置的远端目录。

## 上线前必查（血泪教训）

容器必须注入 `JWT_SECRET` / `AES_KEY`，两者来自 `/opt/swb/swb.env`（**不在仓库内**）。

若 compose 启动时没带 `--env-file`，容器会静默回退到代码内默认密钥，后果是：

1. **全员令牌失效** —— 所有已登录用户被登出；
2. **AES 加密数据解不开** —— SMTP 密码、Webhook 地址等取值为空，通知静默失效。

`release.sh` 已内置防护：找不到环境变量文件、或文件里缺这两个键，直接拒绝发布；
部署后还会回读容器环境变量做自检，未注入则报错退出。

手动重启容器时务必带参数：

```bash
cd /root/GZT && docker compose --env-file /opt/swb/swb.env up -d
```

## 当前状态

- 最新版本：`v0.12.4`
- 已发布版本：v0.1.0 / v0.2.0 / v0.2.1 / v0.3.0 / v0.4.0 / v0.4.1 / v0.4.2 / v0.5.0 / v0.6.0 / v0.7.0 / v0.7.1 / v0.7.2 / v0.7.3 / v0.8.0 / v0.8.1 / v0.9.0 / v0.10.0 / v0.11.0 / v0.11.1 / v0.12.0 / v0.12.1 / v0.12.2 / v0.12.3 / v0.12.4
- 历史说明：v0.1.0 ~ v0.6.0 期间功能与修复混编，未按此规则拆分；v0.7.0 同样混编了修复与新功能（移动端菜单、勾选框、每月任务筛选 + 工号、企业 Logo）。自 v0.8.0 起严格执行「一次发布只做一件事」。

## 版本记录

| 版本 | 类型 | 内容 |
|---|---|---|
| v0.7.1 | 修复 | 导航角标把「逾期 ∪ 今日到期」算成两者相加，2 条任务显示成 4。新增 `due_total`（去重并集），前后端统一取该值 |
| v0.7.2 | 修复 | 提醒里同一个人值多个班次时被重复列出；跨班次去重后本节显示「同上」 |
| v0.7.3 | 修复 | `sendEmailNotify` 在未配置 SMTP 时返回 `nil` 被计为成功渠道，`sent` 虚高。改为哨兵错误 `ErrEmailNotConfigured`，未配置不计入渠道 |
| v0.8.0 | 功能 | 班表新增「所属部门」选择器：超管可指定部门，部门管理员锁定本部门；超管在日历中可见每条排班的部门标签；班次面板与新增表单按所选部门匹配班次配置 |
| v0.8.1 | 修复 | 设置页 SMTP 配置与新增人员表单默认平铺占用大量空间，改为可折叠（SMTP 点标题展开带箭头，新增人员点按钮展开、添加成功自动收起），纯交互调整 |
| v0.9.0 | 功能 | 部门支持二级树（departments 新增 parent_id，破坏性变更）：部门列表/选择器按树展示并可建子部门；数据权限升级为「本部门+全部子孙部门」；删除部门增加子部门/人员校验；任务与班表导入支持指定部门 |
| v0.10.0 | 功能 | 固定 Excel 模板与双格式导入：引入 excelize 支持 .xlsx；班表模板=人×日期矩阵（早/中/晚/夜/休，Sheet「信息」识别年月，重复自动去重）；任务模板=「每日工作内容」+「月度工作内容」两个 sheet（交接班按中班导入）；导入接口按扩展名自动路由；班表页超管可选导入部门 |
| v0.11.0 | 功能 | 站内通知：管理员改动与我相关的信息时推送站内信（含操作人与时间）。覆盖班表新增/修改/删除/批量导入（按人汇总一条）、个人信息字段级变更、密码重置；自己操作自己不通知。导航栏铃铛+未读角标（60s 轮询），抽屉支持单条已读、全部已读 |
| v0.11.1 | 修复 | AutoMigrate 补注册 Notification 表（v0.11.0 通知接口因缺表报错） |
| v0.12.0 | 功能 | ① Logo：新增 ICO 支持（自研 ICO/DIB 解码），PNG/JPG/ICO 上传自动抠纯色背景转透明、限幅 512、统一落地 PNG，移除后回退默认图标；② 任务：单条删除文字化+强确认，新增 `/tasks/batch-delete` 批量删除（逐条返回结果），列表新增部门列与部门筛选（超管）；③ 概览：今日当班改为「部门/姓名/班次」三列（`on_duty_rows`），新增「当月任务」面板与统计卡（月度任务全量）；④ 人员管理：列表显示手机号，编辑/冻结/重置密码/解锁/强制下线/删除收拢进「操作」下拉菜单，新增 `/templates/user-template` 模板下载与 `/users/import` 批量导入（xlsx/csv，登录账号存在则更新）；⑤ 概览布局偏好自动补齐新增卡片 |
| v0.12.1 | 修复 | 人员导入跳过模板尾部的「填写说明」注释行（此前计为 5 条失败）；修复概览窄屏 grid 子项被宽表格撑开（min-width:0） |
| v0.12.2 | 修复 | 移动端背景光斑（.blob 定位在视口外）导致整页横向溢出，`.app-bg` 加 `overflow-x: clip` |
| v0.12.3 | 修复 | 任务 Excel 导入改为**按表头名称匹配列**（不再按固定列号），多余列如「序号」自动忽略、列序不同也能正确识别；无表头时按内容兜底并自动跳过序号列；月度任务年月识别失败不再写入 `0000-00`，改为直接报错；每日任务备注不再硬编码「每日固定工作」（取表内备注列，无则留空）；导入结果回显列识别结果与前 3 条解析示例；批量导入新增显式「导入到部门」选择（此前沿用新建表单的部门，易导错部门） |
| v0.12.4 | 修复 | ① 导入时同一字段出现多列（如「处理人」+「跟进人」）只认最左一列，其余忽略，避免取值随机；② Sheet「信息」兼容横向表头（首行 部门/仓库/年份/月份、次行取值），此前只认纵向的 B3/B4，导致用户交接表年月识别失败、截止日期写成 0000-00 |
